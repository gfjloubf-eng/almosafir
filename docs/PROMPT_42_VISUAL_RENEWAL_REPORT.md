# تقرير P42 «التجديد البصري» — من مظهر تقليدي إلى هوية يليق بها مصمم محترف

**التاريخ:** 2026-09-01 · **الطلب:** «نحسّن المظهر التقليدي ونحوّله لشيء يجعل أب مصمم يقول هذا محترف — واجهات أكثر ترتيباً وجذباً»

## ما تغيّر (بلا هدم — طبقة فوق نظام التصميم الموثق)

| الطبقة | الملف | التجديد |
|---|---|---|
| هوية بصرية 2026 | `wwwroot/css/site.css` (طبقة ملحقة +259 سطراً) | قسم البطل بصورة ليلية أصيلة بألوان النظام، عنوان بنص متدرج، شارة زجاجية، نقاط ثقة ✓، أزرار حبوب متدرجة بارتقاء، دوائر خطوات بحلقات، رقائق أيقونات، دعوة ختام ليلية، **تذييل ليلي جديد** بكامل الصفحات، ظهور تدريجي `.rvl` محترمٌ لـ«تقليل الحركة». |
| واجهة الاستقبال | `Views/Home/Index.cshtml` | إعادة بناء هيكلية كاملة: عيون الأقسام (eyebrow) + عناوين display؛ نفس النموذج والروابط لم تُمس (FromCity/ToCity/Date, asp actions). |
| صورة الهوية | `wwwroot/img/hero-journey.png` | مشهد ليلي أصيل مولّد لهذه المنصة تحديداً: حافلة بليل جبلي، أزور #0284c7 فوق كحلي #0f172a ونقوش زمرد — بلا أي نص. |
| حركة مريحة | `wwwroot/js/site.js` | IntersectionObserver يكشف `.rvl` تدريجياً؛ عطل تام عند prefers-reduced-motion. |

## ثوابت لم تُمس
- فئات النظام القديمة كلها باقية (أي صفحة داخلية لم تُفتح تكتسب التجديد تلقائياً عبر الوراثة).
- صفر تعديل C# أو اختبارات — تحسين عرض خالص؛ البناء يتحقق من Razor في CI.

## ملاحظة صدق
لم تُضف إحصاءات «نافخة» (عدادات سائقين/رحلات مُختلَقة) — أي رقم مستقبلي سيُحقق بمصدر قاعدة حقيقي أو لا يُعرض.
