# قائمة فحص الجاهزية قبل المناقشة والعرض (Presentation Checklist)

---

## 📋 1. فحص بيئة العمل (Environment Readiness)
- [x] تثبيت ورؤية .NET 10 SDK عبر الأمر `dotnet --version`.
- [x] تشغيل خادم XAMPP وتأكيد عمل MySQL على المنفذ `3306`.
- [x] التأكد من وجود قاعدة البيانات `mosafir_db` وجداولها الفعالة.

---

## 🛠️ 2. فحص البناء والاختبارات البرمجية
- [x] تنفيذ `dotnet build AlMosafer.slnx` وتأكيد النتيجة: `0 Errors, 0 Warnings`.
- [x] تنفيذ `dotnet test tests/AlMosafer.Tests/AlMosafer.Tests.csproj` وتأكيد نجاح جميع الاختبارات.

---

## 👤 3. الحسابات الجاهزة للتجربة المباشرة (Demo Accounts)

1. **حساب مدير النظام (Admin)**:
   - البريد الإلكتروني: `admin@almosafir.com`
   - كلمة المرور: `Admin@123456`
   - الصلاحية: لوحة الإدارة الكاملة، إدارة المستخدمين، التقييمات، التقارير البيانية، سلامة النظام.

2. **حساب المسافر (Traveler)**:
   - التسجيل عبر زر "حساب مسافر" في الواجهة الرئسية.
   - الصلاحية: البحث عن رحلات، الحجز، استخراج الإيصال، التقييم، المحادثة.

3. **حساب السائق (Driver)**:
   - التسجيل عبر زر "حساب سائق" في الواجهة الرئيسية.
   - الصلاحية: نشر رحلة جديدة، إدارة الرحلات المضافة، التفاعل مع محادثات الركاب.

---

## 🌐 4. رابط التشغيل المباشر
- **الرابط:** `http://localhost:5163`
