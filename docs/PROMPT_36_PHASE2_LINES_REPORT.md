# PROMPT 36 — المرحلة 2: شبكة الخطوط الرسمية (تقرير إقفال)

**التاريخ:** 2026-08-31 · main = `8cfe9b75` · **CI: run 33436739056 — success** ✅ · 80→**83** اختباراً.
القيد «لا تغيير مخطط» رُفع لهذه الخطوة حصراً بقرار صريح من المالك.

## البنية الجديدة
| الطبقة | المحتوى |
|---|---|
| Domain | `RouteLine` (اسم/مدينة/وصف/تفعيل) + `LineStop` (اسم/ترتيب) + `LineSchedule` (يوم أسبوعي + وقت) |
| Persistence | تكوينات EF بأسماء أعمدة snake_case؛ **هجرة يدوية** `20260831200000_Phase2_RouteLines` (إنشاء 3 جداول بحت، بلا لمس أي جدول قائم) + ModelSnapshot محدثة بنفس النمط |
| Services | `LineService`: عرض عام (مدن → خطوط → تفاصيل) + إدارة (إنشاء، موقف مرتب، موعد أسبوعي بتحقق 0..6 وHH:mm، إيقاف/تفعيل، حذف متتابع) |
| Web | `LinesController` عامة (Index بتصفية مدن، Details) + 6 أفعال إدارة في `AdminController` (كلها POST+Anti-Forgery) |

## واجهات جديدة
- عامة: «شبكة الخطوط 🛣️» بالقائمة + صفحة هرى + تفاصيل خط (مواقف مرقمة وجدول أسبوعي عربي + روابط عميقة للمواصلات الداخلية والبحث).
- إدارية: «الخطوط 🛣️» بإنشاء خط + توسيع مواقف/مواعيد بالسطر نفسه (details/summary) + إيقاف/حذف.

## حوادث وحقائق موثقة بصدق
1. **إعادة بذر بيئية ثانية** منتصف الدور: HEAD قفز إلى `e1e607e` والدفع رُفض. الإصلاح: `fetch` بـrefspec صريح + `reset --soft` لالتزامي غير المنشور (قاعدة التنظيف المصرَّحة) + ترميم 56 ملفاً مفقوداً من القرص من الريموت. **صفر فقدان بيانات** — التحقق النهائي (+1032/−1 على 19 ملفاً = الحزمة وحدها).
2. الهجرة **لم تُجرَّب على MySQL حقيقي** من هذه البيئة (لا SDK/لا قاعدة): الترجمة والاختبارات مُثبتة بـCI؛ التطبيق على قاعدتك الحيّة خطوتك أنت عبر `docs/LINES_PHASE2_UPGRADE.md` (نسخة احتياطية إلزامية أولاً).
3. الربط Trip↔Line **ناعم بالمدينة** في هذه المرحلة (بلا عمود أجنبي على Trip) — موثّق؛ الربط الصلب مرشح لمرحلة لاحقة.
