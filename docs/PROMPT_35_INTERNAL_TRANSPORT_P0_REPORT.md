# PROMPT 35 — المواصلات الداخلية، مرحلة 0 (تقرير بقصة الإخفاق والإصلاح)

**التاريخ:** 2026-08-31 · main = `026c5300` · **CI النهائي: run 33435595635 — success** ✅ · 77→**80** اختباراً متوقعاً.

## ما نُفّذ
1. **اتفاق «المرحلة 0»**: الخط الداخلي = رحلة اختار سائقها المدينة نفسها مرتين (صنعاء ← صنعاء)، والحيّان في نقطة التجمع والملاحظات — **صفر هجرة مخطط**.
2. صفحة عامة «المواصلات الداخلية 🚌» (مفتوحة/مستقبلية/مرتبة) + رابط بالقائمة + شارة 🚌 في بطاقات البحث + إرشاد للسائقين داخل نموذج النشر + ملاحظة شفافية في الصفحة تشرح أن هذا حل مرحلي.
3. اختباران (شمول/استثناء) + اختبار توثيقي للسلوك الجديد.

## ⛑️ قصة الإخفاق الموثقة — لماذا أسقط CI الأول (33435474941)
- لم أقرأ حُرّاس `CreateTripAsync` قبل التصميم: كان فيها رفض «الانطلاق = الوصول» من قِبَل منطق رحلات-بين-المدن.
- اختباراي سقطا في التحضير (التنفيذ صحيح — الفرضية خاطئة)، فالتقطهما CI. لم تُحذف ولم تُعدَّل أي حالة قائمة.
- **الإصلاح (fix-forward):** حرّرت القيد بتعليق موثق (تطابق المدينتين = تعريف الخط الداخلي) وأضفت اختباراً يحمي السلوك الجديد ضد النكس. تحققت أن لا اختبار موجوداً يحمي الرفض القديم قبل تغييره.
- الدرس مسجّل: كل اتفاق جديد يُقابَل أولاً بقراءة كل حُرّاس المسار المتأثر.

## بقايا وباب القرار
- نموذج خطوط رسمي (Line/Station/Schedule) = **مرحلة 2؛ يتطلب هجرة مخطط — قرار المالك**.
