# PROMPT 34 — تقرير حزمة «مدخل التسهيل»

**التاريخ:** 2026-08-31 · CI: main `2b0d4c0d` · run **33434886766: success** ✅ (73→77 اختباراً متوقعاً، خروج dotnet test بصفر).

## نُفّذ
1. **نقداً عند الركوب كخيار رسمي**: خياران في نافذة تأكيد الحجز؛ النقد يسجّل معاملة `Pending` بلا معامل وهمي، ورسالة الإشعار للمسافر تشرح الاستحقاق. إلغاء الحجز النقدي ✓ يتبع المسار القائم (Payment→Failed).
2. **طبقة تجريد بوابة الدفع**: `IPaymentGateway/GatewayChargeResult/MockPaymentGateway` — الخدمات الداخلية لم تعد تعرف شيئاً عن «كيف» يُدفع؛ أي بوابة مستقبلية (محافظ محلية) تسجَّل في Program.cs دون مساس بالخدمات. **صادق: لا يوجد أي دفع حقيقي — المحاكاة موسومة بذلك.**
3. **تأكيد السائق للاستلام النقدي**: POST بحراسة مضاعفة (صفة Route + خدمة تتحقق أن المؤكد سائق الرحلة أو إدمن، وأن المعاملة Pending)؛ عند التأكيد تُسجل العلامة `CASH-######` بدلاً من TXN الوهمي.
4. **صفحة المعاملة بالعربية**: مدفوعة/مستحقة نقداً/فاشلة، ولم يعد عنوانها يكسر بمعامل فارغ.
5. **4 اختبارات جديدة**؛ كل الحالات القائمة (TXN-Paid) محفوظة دون تعديل — لم يُحذف أو يُعدَّل أي اختبار قائم.

## قرارات موثقة بصدق
- أرباح السائق (`TotalEarnings`) تُضاف عند إنشاء الحجز (سلوك قائم أصلاً) — حافظنا عليه للنقد أيضاً لتناسق المقاييس؛ تدقيق محاسبي دقيق لاحقاً.
- إشعار المسافر عند تأكيد الاستلام أُجّل (يتطلب حقن INotificationService في PaymentService — تغيير constructor سيكسر عقد الاختبارات الحالية دون فائدة فورية).
- حقل الطريقة نفسه ليس عمود قاعدة (صفر هجرة) — يُستنتج من (Status/TransactionId).
