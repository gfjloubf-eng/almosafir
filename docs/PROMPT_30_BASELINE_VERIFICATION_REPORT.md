# PROMPT_30_BASELINE_VERIFICATION_REPORT.md

## تقرير إثبات خط الأساس — Area AI
- **التاريخ:** 2026-08-31 | **النطاق:** تحقق + إصلاح موانع الأساس فقط — بلا ميزات، بلا دمج إلى main، بلا دفع، بلا نشر.
- **الالتزامات بالقواعد:** لا حذف PHP، لا تعديل قاعدة بيانات، لا أسرار مطبوعة، لا ادعاء نتائج لم تُنفَّذ.

---

## 1. البيئة
- Debian 12 (x86_64) داخل صندوق رملي. **منافذ الشبكة الصادرة المسموحة: github.com فقط تقريباً** (مُثبت بالتجربة).
- محاولات الحصول على SDK وفشلها (مُوثقة): `dot.net` ✗ SSL_ERROR_SYSCALL · `dotnetcli.azureedge.net` ✗ · `builds.dotnet.microsoft.com` ✗ · `api.nuget.org` ✗ · `download.visualstudio.microsoft.com` ✗ (000) · `aka.ms` ✗ · `deb.debian.org` (لا يصل + Debian 12 لا يحزم dotnet أساساً) ✗. لم أستخدم مرايا غير رسمية (سلسلة إمداد غير موثوقة).
- `docker`/`podman`: **غير موجودَين**. عميل MySQL: غير موجود.

## 2. خط Git الأساسي
- قبل: فرع الجلسة `arena/01a05820-almosafir` = `main` = `e1e607e` (PHP).
- إجراء تحضيري (مسموح): استيراد خط الأساس إلى فرع العمل بـ **`git merge --ff-only 74bab19`** ⇒ fast-forward نظيف (709 ملفات أُضيفت؟ لا — 0 فقدان؛ كل تاريخ PHP محفوظ كسلف). **هذا ليس التوحيد مع main** — main لم تُمَس.
- الحالة الآن: `arena/01a05820-almosafir` = `74bab19` + التزام إصلاحات الأساس (محلي، غير مدفوع).

## 3–4. فرع والتزام C# المرجعيان
`v1.2-chat-api` @ `74bab19` — مُعاد تأكيده ويعمل الآن كقاعدة شجرة العمل.

## 5. إصدار .NET
- **غير متاح في البيئة ⇒ NOT EXECUTED.** المشروع يستهدف `net10.0` (`.csproj` × 5)؛ الحزم: EF Core Design/Pomelo/Identity.Core = `9.0.0` (تنافر محتمل مع net10 — **لم يُتحقق بالبناء**).

## 6. إصدار EF Core
- Pomelo `9.0.0` + EF Design `9.0.0` (من ملفات المشاريع). التوافق مع `net10.0`: **غير مُثبت** — أول ما سيكشفه البناء الحقيقي.

## 7. نتيجة البناء
- **NOT EXECUTED — لا SDK.** لم تُنسخ أي نتيجة من الوثائق. (فحص ساكن تعويضي: توازن أقواس الملفات المعدَّلة بعد تجريد النصوص ✓، JSON الإنتاج صالح ✓، CI YAML مفحوص بصرياً ✓.)

## 8. نتيجة الاختبارات
- **NOT EXECUTED — لا SDK.** العدّ الساكن المؤكد: **59 سمة [Fact]/[Theory]** في 11 ملفاً (≈58 حقيقية + قالب). ❗ الرقم التنفيذي الحقيقي يُنتَظَر من أول run.

## 9. نتيجة النشر
- **NOT EXECUTED — لا SDK.**

## 10. حالة Rate Limiting ← **مُصلَح (تنفيذ فعلي في هذا البرومبت)**
- **المشكلة المؤكدة سابقاً:** سياسة `StrictLimiter` (30 طلب/دقيقة، فيكسيد-ويندو، 429) مُسجلة في `Program.cs` و`UseRateLimiter()` فعّال — **لكن لا `[EnableRateLimiting]` في أي مكان ⇒ كانت معطلة كلياً.**
- **الإصلاح الأدنى المطبَّق:** `[EnableRateLimiting("StrictLimiter")]` على أفعال POST الأربعة الحساسة في `AccountController`: `Login`، `RegisterTraveler`، `RegisterDriver`، `ChangePassword` (+`using Microsoft.AspNetCore.RateLimiting;`). GETs (تصيير النماذج) غير مقيّدة ⇒ لا كسر للاستخدام الطبيعي؛ 429 للمسيء.
- الحالة: **مطبَّق بالكود — التحقق التشغيلي معلّق على البناء.**

## 11. حالة تكوين المدير ← **مُصلَح**
- **المشكلة:** `Program.cs` احتوى fallback بنصّين افتراضيين (`admin@almosafir.com` + كلمة افتراضية ثابتة) — بذرة حسّاسة داخل Git.
- **الإصلاح:** حُذف الافتراضيان نهائياً من الكود. الآن: يُقرأ `AdminSettings:Email/Password` من التكوين/البيئة؛ عند الغياب ⇒ **تخطٍّ آمن مع رسالة تشغيلية واضحة** (لا إنشاء حساب ضعيف ولا سقوط). البذر نفسه ما زال يتحقق من عدم وجود مدير/بريد مكرر ويخزّن **التجزئة فقط** (PBKDF2) — لم يُمسّ.
- بيانات المدير الحقيقية تُمرَّر أسراراً بيئية (`AdminSettings__Email`, `AdminSettings__Password`) — **لم تُطبع في أي ملف/سجل/تقرير.**

## 12. حالة تكوين الإنتاج ← **مُصلَح**
- **المشكلة المؤكدة:** `appsettings.Production.json` احتوى `${DB_HOST}…` حرفيين — **ASP.NET Core لا يوسّع صيغة `${VAR}`** ⇒ كانت ستُقرأ حرفياً وتفشل خفيةً.
- **الإصلاح:** أُزيلت كتلتا `ConnectionStrings` و`AdminSettings` من ملف الإنتاج (القيم تأتي الآن من env حصرياً: `ConnectionStrings__DefaultConnection`…)، وأُضيف في `Program.cs` **فحص fail-fast** يرمي رسالة واضحة لو بقي أي `${` غير موسّع في نص الاتصال.

## 13. حالة CI ← **مُصلَح (تعريفاً)**
- **المشكلة:** المحفّزات كانت `main/master` فقط ⇒ فرع C# لا يُفحَص أبداً.
- **الإصلاح:** `branches: [ main, master, v1.2-chat-api ]` + `workflow_dispatch` (تشغيل يدوي). الخطوات كانت سليمة أصلاً (restore→build→test→publish). **التنفيذ الفعلي: معلّق على أول push** (الدفع ممنوع في هذا البرومبت).

## 14. حالة Docker
- **البناء: NOT EXECUTED** — لا docker/podman في البيئة.
- **الإصلاح المطبَّق:** `.dockerignore` يستبعد الآن `**/*.php`، `db_setup.sql`، `docs/`، `memory-bank/` ⇒ سياق بناء C# نقي، **دون حذف أي ملف PHP من Git**. (ملاحظة دقة: حتى قبل الإصلاح، الصورة النهائية كانت تُبنى من `/app/publish` فقط — التلوث كان سياقياً لا وقت-تشغيلي.)

## 15. حالة تحقق قاعدة البيانات
- **DATABASE RUNTIME VERIFICATION = NOT EXECUTED** (لا عميل MySQL؛ القاعدة الحية على XAMPP لدى المالك). لم يُنفَّذ أي SQL. الساكن المؤكد: الموفر Pomelo 9 + ترحيل `InitialCreate` واحد + DbContext/تكوينات متسقة + fallback MariaDB 10.4.28.

## 16. حالة المصادقة
- ساكنة مؤكدة: كوكي HttpOnly/SameSite=Lax + PBKDF2 + Anti-Forgery على كل POST + Open-Redirect محروس + التحققات المشروحة. بعد هذا البرومبت: + حدّ معدل على الدخول/التسجيل/تغيير كلمة المرور، + لا بذور أسرار. **التشغيلي: معلّق على البناء.**

## 17. حالة التخويل (انحدار)
- المصفوفة الساكنة مؤكدة (Admin/Driver/Traveler على مستوى الأصناف والأفعال، IDOR عبر ResourceOwnershipService وحراس داخل الخدمات، 403 مع AccessDenied عربي). **لم يُغيَّر أي سلوك**. ملاحظة موثقة: `RedirectToDashboard()` يعتمد `User.IsInRole` بعد `SignInAsync` (يعمل لأن المصادقة تبدّل principal في الطلب نفسه) — يخالف نص «الدور الصريح» في الوثائق؛ **يُقترح اختبار حارس** قبل أي لمس مستقبلي. التحقق التشغيلي للانحدار: NOT EXECUTED.

## 18. حالة انحدار الحجز
- لم يُعدَّل شيء. الساكن المؤكد (من التدقيق + إعادة القراءة): معاملة Serializable + فحوص (مفتوحة/مستقبلية/لا-ذاتي/لا-تكرار نشط/مقاعد من المؤكدة) + مبلغ خادمي decimal + دفع وأرباح ومحادثة وإشعاران داخل المعاملة + إلغاء مصرَّح يحرر المقاعد آلياً ويعلّم الدفع Failed. التنفيذي: NOT EXECUTED.

## 19. حالة OperationResult
- موجود ومختبَر وحدياً (اختبارا Success/Failure) — لكن **الخدمات تعيد tuples فعلياً**. لم أُدخل أي تجريد جديد ولم أُوسّع التجريد: **موثَّق كقرار معماري معلَّق** فقط.

## 20. حالة الوسوم
- `v1.3-production-ready` و`v1.3.0-release-candidate` يشيران إلى **`be3742d` (PHP)** — مضللا التسمية لكنهما **لم يُحرَّكا**. توصية الاستراتيجية: بعد التوحيد، وسّم مشروحاً جديداً على main (مثلاً `v2.0.0-csharp-baseline`) وأبقِ الوسوم القديمة كسجل تاريخي.

## 21. حالة التوثيق
- أُضيف واقع مُثبت فقط (هذا التقرير). لم يُكتب «Production Ready»/«Cloud Deployed»/«61 اختباراً» في أي مكان جديد — الأرقام المعتمدة: 59 سمة ساكنة / NOT EXECUTED تنفيذياً / الرابط السحابي معطوب حتى إثبات خلافه.

## 22. الموانع المتبقية
1. **تنفيذ build/test/publish في بيئة .NET 10** (المانع الوحيد الجوهري؛ البيئة هنا تحجب خوادم Microsoft — CI المُسلَّح الآن سيثبتها عند أول push/دمج).
2. إن كشف البناء تنافر EF9/Pomelo9 مع net10 ⇒ ترقية الحزم (تُقَّرر حينها بأدلة).
3. دفع/دمج **ممنوعان حالياً** — يتطلبان برومبت لاحقاً.

## 23. المخاطر المتبقية
- انحدار محتمل غير مكشوف حتى أول بناء حقيقي (توقّعي منخفض: تغييراتي 35 سطراً موضعية ومراجعتها ساكنة نظيفة).
- الوسوم/الفروع المضللة ما زالت قائمة (بقرار: لا نحركها بلا اعتماد).
- بقايا القوالب (`Class1.cs`×3، `UnitTest1`) — تجميلية.

## 24. جاهزية التوحيد
| العنصر | القيمة المؤكدة |
|---|---|
| main الحالية | `e1e607e` (PHP) |
| التزام C# | `74bab19` (+ إصلاحات الأساس على فرع العمل) |
| علاقة النسب | `e1e607e` سلف مباشر لـ `74bab19` ✓ (تاريخ خطي 6 التزامات) |
| نتيجة الدمج المتوقعة | **fast-forward صِرف** من main إلى رأس خط C# |
| تعارضات محتملة | **لا يمكن وقوعها** في ff-only |
| فقدان بيانات | **صفر** — PHP كله سلالة محفوظة |
| الشرط المسبق الوحيد | نجاح CI (build+test) على خط C# |

## 25. الإجراء التالي الدقيق (PROMPT 31 المقترح)
> «PROMPT 31 — PROOF & CONSOLIDATION: ادفع فرع العمل و/أو شغّل CI على `v1.2-chat-api`، وثّق مخرجات build/test/publish الفعلية (Passed/Failed/Total + زمن)، عالج أي فشل مكشوف (الترقية الحزمية إن لزم)، ثم نفّذ `git checkout main && git merge --ff-only <رأس خط C# المُثبت>`، وسمِّ `v2.0.0-csharp-baseline`، وافتح ملاحق التوحيد (أرشفة PHP بقرار، اختبار حارس للتوجيه-بالدور).»

---

# القرار النهائي

## 🟡 B — READY AFTER MINOR FIXES

**التسويغ بالأدلة:**
- كل موانع *الكود* المؤكدة من PROMPT 29 أُزيلت فعلياً وبدليل التعديل (Rate limiting مفعّل الآن ✎، لا أسرار افتراضية ✎، إنتاج بلا `${...}` زائف ✎، CI مسلَّح على فرع C# ✎، Docker نظيف السياق ✎) — 5 ملفات، ‎+41/−15 سطراً، بلا لمس لأي منطق أعمال.
- التوحيد آمن بنيوياً: ff-only، لا تعارض، لا فقدان.
- **ما منع A:** حُكم «خط أساس مُثبت» يتطلب بناءً واختباراً *منفَّذَين* فعلاً — وهذا مستحيل مادياً في هذه البيئة (مصادَرات Microsoft محجوبة). لم ولن أختلق نجاحاً. A يُعلَن فور نجاح أول run حقيقي (CI سيقوم بها آلياً عند أول دفع مصرَّح).

*توقفت هنا: لا دمج، لا دفع، لا نشر، لا ميزات. جميع التغييرات محلية على فرع الجلسة وقابلة للمراجعة بـ `git diff`.*
