# PROMPT 38 — حزمة «الهوية» (تقرير إقفال)

**التاريخ:** 2026-08-31 · main = `7c49f9f4` · **CI النهائي: run 33439348146 — success** ✅ (فشل أول 33439073453 أُصلح fix-forward) · 86→**90** اختباراً.

## نُفّذ
- `IEmailService`/`EmailService` عبر **MailKit 4.8** (في `Smtp:*` من الإعدادات/متغيرات البيئة `Smtp__Password`) — **فاشل-آمن**: بلا إعدادات يتخطى الإرسال بصمتٍ مسجَّل ببريد مقنَّع، ولا يسقط النظام أبداً.
- **استعادة كلمة مرور حقيقية**: رابط برمز 64-حرفاً، صلاحية 30 دقيقة، **استخدام واحد** (يُمحى فور النجاح)، تخزين في `PreferencesJson` (صفر هجرة)، حماية استعلام الوجود (نفس الرسالة للبريد المجهول)، وRateLimiting على أفعالها.
- صفحتا `ForgotPassword`/`ResetPassword` + رابط في تسجيل الدخول + كتلة Smtp موثقة في `appsettings.Example.json`.

## حادثة موثقة بصدق
- فشل البناء الأول: ثغرة أكلِ-الهروب في سكربتاتي (`\"` → `"` أفرز `Contains(""PasswordReset"")` غير القانوني + سمات Login انزاحت لحظة الإدراج — أُصلحت قبل الترجمة أصلاً).
- الدرس المدوَّن: كل حقن C# بأوتاد نصية يُعاد قراءته بالبايتات (`cat -A`/read_file) قبل الإقلاع.

## للوصول إلى 10 — انظر docs/ROADMAP_TO_10.md (المصدر الوحيد للخطة).
