# خطة P43 «جولة التلميع» — كيف نصلح عيوب الواجهات الـ12 كاملة

**التاريخ:** 2026-09-01 · **المرجع:** تقييم الواجهات الصارم (الدرجة 7.2/10 — الرئيسية 8.5).

## منهجية العمل
- **صفر كسر:** كل موجة لا تلمس منطقاً ولا اختبارات؛ عرض خالص (views/CSS/JS) + تحقق CI.
- **ترتيب القيمة:** الأثر البصري الأعلى مقابل المخاطرة الأدنى أولاً.
- **دليل لكل إصلاح:** كل سطر أدناه يشير للدليل الذي وُثّق في التقييم (عدّ ملفات/لقطات).

## الموجة ١ — مشحونة الآن ✅

| العيب | الإصلاح المُنفَّذ | الملف |
|---|---|---|
| 3) خلط 16 ملف `btn-primary/secondary` خام | **إعادة تعريف موحّدة** فوق توكناتنا (حبوب متدرجة، ارتقاء) — كل الصفحات تصبح متسقة دفعة واحدة دون فتح 16 ملفاً | site.css |
| 1) زحام «دخول + حساب مسافر + حساب سائق» قبل التسجيل | زر وحيد «انضم الآن ✨» منسدل + «تسجيل الدخول» | _Layout.cshtml |
| 5) favicon قديم (قالب VS) | favicon.ico مُولَّد من شعارنا (16/32/48px) + رابط صريح | favicon.ico + head |
| 2) حقل التاريخ بالإنجليزية | **معلّق لموجة ٢:** العرض يتبع لغة المتصفح لا صاحب الموقع؛ الحل: flatpickr مورَّد (قناة npm نفسها التي أنجبت SignalR) أو حقل نصوص. قرار موازنة أمان/جمال | — |
| 8) لا اقتراحات مدن | `datalist` حية من شبكة الخطوط المعتمدة (قاعدة بياناتك فعلاً) + حقول بـlist | HomeController + Index |
| 9) لا إحساس سرعة | شريط تقدم علوي متدرج يقفز عند أي نقرة داخلية | _Layout + site.js |
| 10) حافلة الجوال تختفي | إعادة قص الخلفية (62% center) ليبقى الباص في الكادر | site.css |
| — | تنبيهات اللحظة بأنماط مضمّنة | فئة `.alm-toast` بنفس التوكنات | realtime.js + css |

## الموجة ٢ — بعد موافقتك

| العيب | الإصلاح المخطط | المخاطرة/الكلفة |
|---|---|---|
| 4) إيموجي كإيقونات (29 عنواناً) | نواة أيقونات SVG موحّدة (Lucide مفتوحة) عبر مساعد Razor؛ الإيموجي يبقى للنص الودّي فقط | متوسطة — مس شاشات كثيرة، صفر منطق |
| 6) لا ترقيم صفحات | مكوّن `data-paginate` عام: ترقيم فوري لكل قائمة (جدول/بطاقات) بعشرة عناصر/صفحة — عميلاً بدون لمس الخدمات | صفر كسر اختبارات |
| 2) التاريخ بالإنجليزية | flatpickr من npm — عربي RTL بتنسيق yyyy-mm-dd | منخفضة — ملفان مورَّدان |
| 7) بحث بطل مكرر في الشريط | إخفاء رابط «البحث عن رحلة» عند الرئيسية فقط | تافهة |

## الموجة ٣ — الصقل النهائي
| العيب | الإصلاح |
|---|---|
| 11) وسام إيموجي للمتصدرين | دوائر أحرف أولى بتدرج لوني من بصمة اسم السائق + شارة «موثّق» |
| 12) إيقاع الصفحات الداخلية | مسح شامل: عناوين موحدة (`.eyebrow-text + .section-title`) ومسافات متسقة عبر ~40 عرضاً |

## التحقق بعد كل موجة
1. CI أخضر (البناء يتحقق من Razor).
2. جولة يدوية لديك: Ctrl+F5 → نقاط القبول.
3. تقرير مستقل في docs/.

## ملاحظة صادقة (حدود اليوم العميقة)
زحام الروابط بعد تسجيل الدخول جوهري لأن الأدوار الثلاثة تملك أدوات فعلية مختلفة؛ الحل الحقيقي = شريط جانبي للمسجَّلين — قرار هيكلي جديد يستنزف إعادة تصميم تخطيط كاملة، لذا يحتاج ضوءاً أخضر صريحاً منك.
MD