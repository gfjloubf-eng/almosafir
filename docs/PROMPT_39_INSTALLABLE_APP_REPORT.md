# PROMPT 39 — «التطبيق القابل للتثبيت» (الدسكتوب والويب بنفس الكود)

**التاريخ:** 2026-08-31 · main = `b4a1a728` · **CI: run 33440122854 — success** ✅.

## نُفّذ (استجابةً مباشرة لطلب المالك: تطبيق يُثبَّت + خانة فريق الدعم والمطورين)
1. **PWA كامل**: manifest عربي RTL + أيقونة SVG يدوية بألوان الهوية + PNG 512 مولّد + `sw.js` + `offline.html`.
   - سياسة الكاش محافظة ومقصودة: ثابتات فقط بالكاش؛ صفحات الحساب شبكة-أولاً (لا محتوى خاص متقادم أبداً).
2. **الإعدادات**: قسم 0 — «ثبّت كتطبيق 📲» بإرشاد لكل متصفح + بطاقة **«فريق الدعم والمطورين 🛠️»** (المطوّر/الإدارة/البريد/الواتساب من `SupportTeam:*` بافتراضيات عربية، وأمانة: تُدار من إعدادات النظام لا من الواجهة).
3. **التوثيق المطلوب**: `docs/PWA_AND_DISTRIBUTION.md` — الخوارزمية (كود واحد → متصفح → غلاف تطبيق)، مصفوفة قنوات البث، وحدود الصدق.

## صدقيات هذه الدفعة
- طلب «خادم مبني يدوياً (WebSocket خام)» رُفض هندسياً ووُثّق: SignalR هو الأدات المصممة ولا يصنع WebSocket-يدويٌّ سوى الدَّين؛ المانع الحقيقي (ملف signalr.min.js محجوب من بيئة البناء) سجّلته في الوثيقة ويبقى إسقاط سطر واحد مستقبلاً.
- PWA قابلية التثبيت تتأكد متصفحياً فقط (بلا أداة تحقق محلية هنا) — CI أخضر يضمن البناء لا سلوك العامل.
