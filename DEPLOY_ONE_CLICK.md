# النشر بلمسة واحدة — «بوابة النشر الحر» (P47.5)

> **تاريخ الإطلاق التقني:** 2026-09-01 · **حالة CI:** أخضر على `main`.

## أ) نسخة معاينة مجانية حية (5 دقائق — بلا بطاقة دفع)

1. افتح لوحة Render: **https://dashboard.render.com** (حساب مجاني — تسجيل بـGitHub مباشرة).
2. زر **New → Blueprint**.
3. اربط حساب GitHub واختر المستودع: **`gfjloubf-eng/almosafir`** (فرع `main`).
4. Render يقرأ `render.yaml` ويكتشف `Dockerfile` — اضغط **Apply**.
5. بعد ~4–6 دقائق بناء تحصل على رابط بالشكل: `https://almosafer.onrender.com` ✅ **هذا هو رابطك العام**.

### دخول الأدمن في النسخة التجريبية
- البريد: `admin@almosafer.preview`
- كلمة المرور: **توليد عشوائي آمن** — من Render: الخدمة → **Environment** → `AdminSettings__Password` → أيقونة العين.
- غيّرها بعد أول دخول (الملف الشخصي ← تغيير كلمة المرور).

### حدود صادقة للطبقة المجانية
- تنام بعد 15 دقيقة خمول؛ أول زيارة توقظها (≈30 ثانية).
- القرص مؤقت: **بيانات SQLite تُصفَّر عند كل نشر جديد** — مثالية للعرض والتجربة، لا للإنتاج.

## ب) تشغيل محلي فوري على جهازك (أي نظام — بلا XAMPP)

```bash
set DbProvider=sqlite
set DemoSeed=true
dotnet run --project src/AlMosafer.Web
```
يُنشئ `almosafer-preview.db` تلقائياً ويبذر الأدمن إن ضبطت `AdminSettings__Email/Password`.

### بذرة العرض التقديمي (`DemoSeed=true`)
تملأ المنصة بمحتوى يمني واقعي جاهز للعرض: 3 سائقين + 2 مسافرين + 6 خطوط داخلية (صنعاء/عدن/تعز/إب/الحديدة/ذمار بمواقف ومواعيد) + 6 رحلات بين المدن بأسعار بالريال + حجوزات وتقييمات تُحيي قسم المتصدرين.
- حسابات العرض كلها تنتهي بـ`@demo.almosafer` وكلمة مرور موحدة `Demo@12345` — **للاستعراض المحلي فقط، لا تنشرها إنتاجياً**.
- آمنة التكرار: تتخطى نفسها إن كانت قاعدتك تحوي خطوطاً مسبقاً.
- للإفراغ والبدء من جديد: احذف `almosafer-preview.db` وأعد التشغيل.

## ج) الانتقال للإنتاج الحقيقي لاحقاً (القاعدة لا تتغير)

1. أزل متغير `DbProvider` (أو اجعله غير `sqlite`).
2. أضف `ConnectionStrings__DefaultConnection` لـMySQL/MariaDB خارجي دائم.
3. نفس الكود، نفس الميزات — الهجرات الذكية تشتغل تلقائياً كما في جهازك تماماً.

## مبدأ التصميم
بوابة SQLite **مفصولة بمتغير بيئة فقط** (`DbProvider=sqlite`)؛ كل مسار MySQL الإنتاجي بقي حرفاً واحداً دون تغيير، وCI يبني المسارين معاً.
